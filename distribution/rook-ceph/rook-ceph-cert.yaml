apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rook-ceph-ingressgateway-certs
  namespace: rook-ceph
spec:
  secretName: rook-ceph-ingressgateway-certs
  issuerRef:
    name: gateways-issuer
    kind: ClusterIssuer
  commonName: rook-ceph.dev.csgroup.space
  dnsNames:
    - rook-ceph.dev.csgroup.space
